# Cherimoya PRO-cap models

These models were trained while `cherimoya` was in its early development stages. The initial models were trained using the git commit id `69f16dc7ff48ad094aafd4b93433972181c65d50`, which is not compatible with newer releases. Please checkout that specific commit if you wish to use the initial set of models.